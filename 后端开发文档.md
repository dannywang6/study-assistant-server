# study-assistant-server 后端开发文档

## 一、项目概览

| 项 | 内容 |
|---|---|
| 技术栈 | Spring Boot / MyBatis / MySQL / Lombok / MapStruct |
| 数据库 | `study_assistant`（localhost:3306，`root/098098098`） |
| 配置 | `src/main/resources/application.yml` |
| Mapper XML | `src/main/resources/mapper/*.xml` |
| 包结构 | `controller / service / mapper / entity / dto / converter / common` |

### 现有表结构

`activity_record`
| 列 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK 自增 | |
| app / title / url | VARCHAR | 原始事实（Agent 上报） |
| category / subcategory | VARCHAR | **Agent 已不再下发，入库时由后端分类填写** |
| start_time / end_time | DATETIME | |

`idle_record`
| 列 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK 自增 | |
| activity_id | BIGINT | 关联 activity_record.id |
| start_time / end_time | DATETIME | |

## 二、分层与查询链路

```
Controller（接收参数）→ Service（业务/组装/分类）→ Mapper 接口 → Mapper XML（SQL）→ MySQL → 返回 JSON
```

现有文件分布：
- `controller/AgentStatusController.java`：`POST /api/agent/status`（心跳，目前只打印）、`POST /api/agent/report`（活动上报打印，临时，最终该改回 `ActivityController`）
- `service/ActivityService + impl`：上报入库的核心
- `mapper/ActivityMapper(+xml) / IdleMapper(+xml)`：insert 语句已就绪

## 三、与 Agent 的对接现状

| 数据 | Agent 请求 | 后端现状 |
|---|---|---|
| 心跳 CurrentStatus | `POST /api/agent/status`，每 5s | 只打印，未存储 |
| 活动上报 | `POST /api/activity/report` | 入库（需恢复正式 ActivityController） |
| 上报 JSON 含 `eventId` | — | 后端暂忽略（Spring 默认忽略未知字段，不报错） |

## 四、分类设计（判断「学习/娱乐」放在后端，方案 B）

### 4.1 原则

分类**不再由 Agent 下发**，改由后端在**入库时**判断并落库到 `category / subcategory` 列。查询方永远只用 SQL 按分类聚合，不再关心"怎么分类"。

### 4.2 入口位置（Service 层）

在 `ActivityServiceImpl.saveActivities()` 里，每条活动写库之前调用分类器：

```
POST /api/activity/report
   → ActivityController
   → ActivityService.saveActivities(requests)
   → 对每条 req: 调 classifier.classify(app, title, url)
                       ↓
                拿到 category / subcategory
   → 设置进 ActivityRecord   ← 此时才赋值
   → activityMapper.insert()  ← 带着分类一起入库
```

### 4.3 规则数据源（方案 B：JSON 配置文件）

规则写在 `src/main/resources/category-rules.json`，**改规则无需重编译**，后续前端可做配置页直接读取/修改此文件。服务启动时加载进内存（`@PostConstruct` 读一次，或定时刷新）。

规则结构（样例）：

```json
{
  "appRules": [
    { "app": "IntelliJ IDEA", "category": "学习", "subcategory": "编程" },
    { "app": "Chrome", "category": "", "subcategory": "" },
    { "app": "Bilibili", "category": "娱乐", "subcategory": "视频" }
  ],
  "titleRules": [
    { "keyword": "SpringBoot", "category": "学习", "subcategory": "Java" },
    { "keyword": "MyBatis", "category": "学习", "subcategory": "Java" },
    { "keyword": "日语", "category": "学习", "subcategory": "日语" }
  ],
  "urlRules": [
    { "keyword": "youtube.com", "category": "娱乐", "subcategory": "视频" },
    { "keyword": "bilibili.com", "category": "娱乐", "subcategory": "视频" }
  ]
}
```

### 4.4 分类判定逻辑（优先级）

按优先级匹配，先 app、再标题关键词、再 URL，兜底 null（"其他"交由前端兜底）：

```
1) app 精确匹配        IntelliJ IDEA → 学习 / 编程
                       GTA V        → 游戏 / 游戏
2) title 关键词        标题含 "SpringBoot"/"MyBatis" → 子分类 Java/后端
3) url 关键词          youtube.com / bilibili.com → 娱乐 / 视频
4) 都匹配不上 → category 留 null（"其他"由前端兜底）
```

规则设计要点：
- `appRules` 中 `category` 为空表示"不能只看 app 下结论，继续看 title/url"（如 Chrome）。
- 命中 app 规则且 category 非空，直接返回，不再往下。
- title/url 按关键词子串匹配（contains）。

### 4.5 需要的新文件

1. `service/CategoryClassifier.java`（接口）：`Category classify(String app, String title, String url);`
2. `service/impl/CategoryClassifierImpl.java`：加载 `category-rules.json` + 按优先级匹配
3. `model/Category.java` 或直接用二字段对象：`{ category, subcategory }`
4. `src/main/resources/category-rules.json`：默认规则
5. 恢复 `controller/ActivityController.java`：`POST /api/activity/report` 接回 `ActivityService`
6. 修改 `ActivityServiceImpl.saveActivities`：写库前设置分类

### 4.6 后续：前端可修改配置文件

V2 做 Web 配置页时，可提供 `GET/PUT /api/category/rules` 读取/更新规则，保存后写回 `category-rules.json`（或后续改存 `category_rule` 表），服务端支持热刷新。V1 只需保证规则由配置文件驱动。

## 五、V1 五个查询接口

### ① 万能历史查询

```
GET /api/activity/records
参数: startTime, endTime, category, subcategory, app, page, size
返回: { total, page, size, list: [ {id,app,title,url,category,subcategory,startTime,endTime} ] }
```

SQL：`WHERE start_time>=? AND end_time<=?` 叠加可选条件（category/subcategory/app），`ORDER BY start_time DESC LIMIT ? OFFSET ?`，另查 `COUNT(*)`。这是所有统计的基础。

### ② 单条 Activity + Idle 详情

```
GET /api/activity/{id}
返回: { id,app,title,url,category,subcategory,startTime,endTime, idleRecords:[...] }
```

SQL：`activity_record WHERE id=?` + `idle_record WHERE activity_id=?`。

### ③ 今日活动时间线

```
GET /api/activity/today/timeline?date=2026-09-13   (默认今天)
返回: [ {startTime,endTime,app,title,category,subcategory} ] 按 startTime 升序
```

SQL：`WHERE start_time >= 当天00:00 AND start_time < 次日00:00`。

### ④ 今日分类统计

```
GET /api/statistics/today
返回: [ {category, totalSeconds} ]  例: [{"category":"学习","totalSeconds":13320},...]
```

SQL：

```sql
SELECT category, SUM(TIMESTAMPDIFF(SECOND, start_time, end_time)) AS total_seconds
FROM activity_record
WHERE start_time >= '2026-09-13 00:00:00' AND start_time < '2026-09-14 00:00:00'
GROUP BY category;
```

依赖分类服务（第四节）落库后才有意义。

### ⑤ 当前状态

```
GET /api/dashboard/current
返回: { app,title,url,category,subcategory,startTime,elapsedMinutes,idle }
```

数据源 = 心跳内存状态（`CurrentStatusService` 的 `ConcurrentHashMap`），`elapsedMinutes = now - activityStartTime`，`online` 由 lastSeen（>=60s 未收到即离线）判定。

## 六、心跳接收规划

- 心跳 5s 一笔，**不写 MySQL**，只存内存（`ConcurrentHashMap<String, CurrentStatus>`，key=agentId）。
- 新增 `service/CurrentStatusService` + impl，供 `/api/dashboard/current` 读取。
- 新增 `GET /api/agent/status/{agentId}`：返回该 agent 当前状态（含 online/lastSeen），方便验证。

## 七、V2 后续接口（先不用做）

```
GET /api/dashboard/today/goals         目标总览
GET /api/dashboard/today/summary       今日概览（学习/日语/游戏/娱乐/其他/Idle）
GET /api/schedule/today                今日计划日程
GET /api/statistics/today/subcategories 今日子分类（ECharts）
GET /api/statistics?startTime&endTime   任意时间段统计（昨天/7天/30天复用）
GET /api/activity/{id}/idle            仅 Idle
GET /api/summary/daily?date=           每日总结（供 AI 生成文字）
GET /api/summary?type=week&startDate=  周/月/季/年统一
GET/PUT /api/category/rules            分类规则配置管理（前端改规则）
WebSocket                              ACTIVITY_STARTED / IDLE_STARTED / GOAL_COMPLETED...（以后）
```

## 八、V1 开发顺序

1. 恢复 `ActivityController`（POST report 接回 ActivityService，打通入库链路）
2. 实现 `CategoryClassifier` + `category-rules.json`，在 `saveActivities` 里落分类（**让老数据也能补分类：可对已入库 null 分类的记录做一次回填**）
3. `GET /api/activity/records`（分页+条件）
4. `GET /api/activity/{id}`（含 idle）
5. `GET /api/activity/today/timeline`
6. `CurrentStatusService` 内存表 + `GET /api/agent/status/{agentId}`
7. `GET /api/dashboard/current`
8. `GET /api/statistics/today`
9. 再进入 V2

## 九、验收（curl）

```bash
curl "http://localhost:8080/api/activity/records?startTime=2026-09-13T00:00:00&size=50"
curl "http://localhost:8080/api/activity/1"
curl "http://localhost:8080/api/activity/today/timeline"
curl "http://localhost:8080/api/dashboard/current"
curl "http://localhost:8080/api/statistics/today"
curl "http://localhost:8080/api/agent/status/{agentId}"
```